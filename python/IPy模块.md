# 安装

```bash
pip install IPy
```

该模块一般情况只用上IP类，导入方法：from IPy import IP

# IP类

保存ip对象，可以解析出IP的一些参数。

# reverseName()方法

返回反向解析地址格式。

# iptype()方法

返回IP类型。

# int(),strHex(),strBin()方法

转换IP为整形，十六进制，二进制

# netmask()方法

返回子网掩码。

